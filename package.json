{
  "name": "dehouse-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "cd dg-house-landing && npm install --legacy-peer-deps && npm run build",
    "start": "cd dg-house-landing && npm install --legacy-peer-deps && npm run start"
  }
}