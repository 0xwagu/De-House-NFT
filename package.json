{
  "name": "dehouse-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "cd dg-house-landing && yarn install && yarn build",
    "start": "cd dg-house-landing && yarn install && yarn start"
  }
}